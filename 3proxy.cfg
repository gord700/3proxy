# Optimized for Koyeb Free Plan - HTTP Only
log /dev/stdout D
logformat "L%t %U %R %r %b"
auth none
allow *
proxy -p3128 -i0.0.0.0  # HTTP proxy, bind to all interfaces
# SOCKS removed for free plan compatibility
